#include<stdio.h>
int main()
{
    float a = 77; // physics marks 
    float b = 94; // maths marks
    float c = 87; // english marks
    float d = 65; // urdu marks
    float e = 99; // chemistry marks

    float p = (a + b + c + d + e) /5;
    printf("%f",p);

    return 0;
}