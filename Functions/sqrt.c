#include <stdio.h>
#include <math.h>
int main()
{

    // int n;
    // printf("Enter Number :");
    // scanf("%d", &n);

    // int a;
    // printf("Enter Number :");
    // scanf("%d", &a);

    // int root = sqrt(n);
    // printf("The square root is : %d", root);

    // int q = pow(3, 3);
    // printf("%d", q);

    int q = min(3, 1);
    printf("%d", q);

    return 0;
}